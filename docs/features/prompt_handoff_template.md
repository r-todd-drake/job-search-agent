# Prompt Handoff — [Feature Title]

## Session Goal
[One or two sentences. What should be built or completed by the end of this session?]

## Context Files to Load
[List files CC should read before starting. Include paths relative to project root. Note which are required vs. reference.]

| File | Purpose | Required? |
|---|---|---|
| `docs/features/[feature-name]/proposal.md` | User story and acceptance criteria | Yes |
| `docs/features/[feature-name]/[spec].md` | Design spec -- reviewed and approved | Yes |
| `[other context file]` | [why it's relevant] | Yes / Reference |

## Where to Begin
[Name the first task explicitly. CC should not have to infer starting point from the spec.]

## Session-Specific Notes
[Anything not covered in the spec that CC needs for this session only -- e.g., a known constraint, a preferred order of operations, a file that has changed since the spec was written. If there's nothing to add, write "None."]
- This plan contains `⚠ REVIEW:` annotations. Resolve each before proceeding past the annotated step.

## Definition of Done
[How CC and the user will know this session is complete. Should map directly to the acceptance criteria in proposal.md -- reference them by name rather than restating them.]

---

*This handoff was written after spec review was complete. All `⚠ REVIEW:` items in the spec are resolved. See `✅ RESOLVED:` annotations in the spec for decisions made during review.*
