# [Feature Title]

## User Story and Acceptance Criteria

### User Story
"As a [type of user], I want [an updated capability] so that [benefit]."

### Acceptance Criteria (AC)
[Detailed, testable conditions that define when the story is done. Each criterion should be independently verifiable -- avoid compound criteria. Group by functional area if the feature has multiple sub-capabilities.]

### Out of Scope
[Clearly list what this feature does not include. Prevents scope creep during build and review. If a related capability is deferred to another feature, name it explicitly.]

---

## Review Annotations
*This section is populated during the Chat spec review step (README process step 4). Do not fill in manually.*

Open items use `> ⚠ REVIEW:` and must be resolved before build starts.
Resolved items use `> ✅ RESOLVED:` and document what was decided.
